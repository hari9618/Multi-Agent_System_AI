# NexusAI Multi-Agent System — Requirements v4.0
# ================================================
# Install: pip install -r requirements.txt
#
# IMPORTANT FIX: httpx must be pinned below 0.28.0
# httpx>=0.28.0 removed the 'proxies' argument that groq still passes internally.
# This causes: "Client.__init__() got an unexpected keyword argument 'proxies'"

# ── HTTP client (pin FIRST before groq installs it) ──
httpx>=0.27.0,<0.28.0

# ── Core UI ──
streamlit>=1.35.0

# ── LangChain ecosystem ──
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=0.1.0

# ── Groq LLM provider ──
# Model used: llama3-8b-8192  (fast, active)
# DO NOT use llama3-70b-8192 — it is DECOMMISSIONED by Groq
langchain-groq>=0.1.5
groq>=0.9.0

# ── FAISS vector memory (optional — graceful fallback if missing) ──
faiss-cpu>=1.7.4
sentence-transformers>=2.7.0

# ── Web search tool (optional — set TAVILY_API_KEY in .env to enable) ──
tavily-python>=0.3.3

# ── Utilities ──
python-dotenv>=1.0.0
